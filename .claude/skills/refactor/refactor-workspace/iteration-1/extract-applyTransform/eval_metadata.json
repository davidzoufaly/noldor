{
  "eval_id": 2,
  "eval_name": "extract-applyTransform",
  "prompt": "Extract the transform application logic from the engine into a cleaner API — the applyTransform function is doing too much, break it into smaller composable pieces",
  "assertions": [
    {
      "id": "multiple-functions-extracted",
      "text": "At least 2 new focused functions were extracted from applyTransform"
    },
    { "id": "typecheck-passes", "text": "pnpm typecheck passes after the refactoring" },
    { "id": "tests-pass", "text": "pnpm test passes after the refactoring" },
    { "id": "report-has-diffs", "text": "Report contains diff blocks for changed files" },
    {
      "id": "report-has-rationale",
      "text": "Each extraction has a specific rationale explaining why"
    },
    { "id": "report-has-complexity-delta", "text": "Report shows max function length decreased" },
    { "id": "report-has-dead-code-section", "text": "Report includes a dead code check section" }
  ]
}
