node_modules/
.worktrees/
dist/
*.tsbuildinfo
.turbo/
coverage/

# Per-user local inbox (mirrors Charuy root)
docs/ideas.md

# Generated rule index
.noldor/.rules-index.json

# Operator-local session + gate state (per-machine, never committed)
.noldor/session.json
.noldor/release-state.json
.noldor/overrides.log
.noldor/cr-overrides.log
.noldor/dashboard.log
.noldor/garden-receipt
.noldor/release-pushes.log
.noldor/cr/
.noldor/drain-state.json
.noldor/drain.lock
.noldor/drain.lock.reclaim.*
.noldor/drain-stop
.noldor/drain.pause
.noldor/drain-*.log
.noldor/escalations.jsonl
.noldor/drain-park.json
.noldor/watch-state.json
.noldor/watch.log
.noldor/watch.pid
.noldor/agent-events.jsonl
.noldor/agent-events.archive.jsonl
.noldor/ideas-recovery-*.md
.noldor/prep-batch/
.noldor/research/

# Harness scheduler lock (operator-local runtime state)
.claude/scheduled_tasks.lock

# Machine-local graphify interpreter path (resolved per-run)
graphify-out/.graphify_python
# Machine-local graphify caches + per-run artifacts (regenerated each run)
graphify-out/cache/
graphify-out/cost.json
graphify-out/.graphify_incremental.json
# Generated HTML viz — 41k-line oxfmt/git churn per sweep, not for review (graph-integration.md)
graphify-out/graph.html
# Stray graphify output dumped under a scanned source dir (e.g. src/graphify-out/) — never
# commit. Nested-only (`*/`) so the tracked top-level graphify-out/ stays addable.
*/graphify-out/
metrics.json
# playwright-cli skill scratch — console logs + page snapshots from browser
# automation (dashboard visual verification); transient, never for review.
.playwright-cli/

# noldor transient state (added by noldor init)
.noldor/session.json
.noldor/agent-events.jsonl
.noldor/agent-events.archive.jsonl
.noldor/cr/
.noldor/drain-state.json
.noldor/drain.lock
.noldor/drain-park.json
