# Ideas

Raw entry point for human-generated ideas. `/triage` promotes bullets into `docs/roadmap.md` (flat priority-ordered list) or `docs/backlog.md` (parking lot).

- 3 verticals: tooling, business, core product
- roadmap: flat priority-ordered list (file order = priority); H3 categories group related entries

## Notes

## Priority

## Not groomed

- Multiagent works -> support multiple branches and multiple worktrees and multiple terminal tabs -> reflect in dashboard and agents tab (model agnostic) -> backlog this one
- when there are X + XL tasks split them to smaller ones in which Phase? The goal is to work with the smallest context possible -> important!
- the codex lane cannot review code: `src/cr/lanes/codex.ts:58` maps `kind === 'spec' ? '--spec' : '--plan'`, so `--kind code` passes `--plan` with a source path and `runPlanReview` applies **plan-review heuristics to TypeScript**. Observed by opting codex into the code stage while shipping Q-0089: it returned 5 high "blockers" complaining that `src/cr/codex-spawn.ts` "lacks explicit goal, scope, affected files, implementation steps, TODO checklist, output location" and labelled every finding's file `Plan to review`. Not review output at all. **This blocks Q-0091** (mandatory codex round on bigger tasks), which would wire codex into `crLanes.code` and get nonsense on every run. Fix: give the lane a code path — build a diff context like `buildContext` does for the gate lane and pass the code-review prompt (`run-codex.ts` already has `CodeReviewCtx` for exactly this), rather than falling through to `--plan`. (surfaced shipping Q-0089)
- `codexSupportsBaseSha()` can never return true, so codex artifact review is **always full-scope**. It runs `pnpm --silent noldor cr codex --help` and greps for `--base-sha`, but the CLI dispatcher intercepts `--help` first: `src/cli/help.ts:25` prints `Usage: noldor cr codex [args]` plus the one-line manifest desc and returns, so the detailed `USAGE` string in `src/cr/codex.ts` (which does list `--base-sha`) is unreachable and `runCli`'s `inv.help` branch is dead code. Measured: the probe exits 0 in 307 ms with zero matches, so every run logs `--base-sha unsupported by installed CLI — fall back to full review` and the `baseSha` field never lands in a sink. This is the live mechanism behind Q-0089's symptom (a); the spec's account (a bad sha throwing in `git diff`) is true but describes a different path. Fix is a judgement call: make the dispatcher delegate `--help` to a subcommand that owns a detailed usage string (touches every subcommand), or replace the help-grep probe with a version check. Deliberately not fixed inside Q-0089 — the correct fix is a change to the shared CLI help surface. (surfaced shipping Q-0089)
- `spawnCodex` accumulates stdout/stderr unbounded in memory. Fine for codex's measured ~326 KB, but a runaway child could grow the node heap without limit; the bounding that exists (`formatStderrTail`, 4000 chars) applies only to what reaches the sink. Deliberately not fixed with Q-0089 — no measured case, and the outer `execFile` no longer bounds it since the inner spawn owns the streams. Worth a cap if a runaway ever shows up. (surfaced shipping Q-0089, raised by the codex lane)
- `cr aggregate` reads a **missing** expected sink as green. It discovers sinks by listing `.noldor/cr/` and prefix-matching (`src/cr/aggregate.ts:36-40`) and holds no expected-lane set, so a lane that never wrote a sink is invisible rather than `unresolved` — that list is only populated for a sink that exists and lacks `finishedAt` (`aggregate.ts:107`). With no other blockers, `ok: blockers.length === 0 && unresolved.length === 0` is therefore true, so a lane killed before it could write is indistinguishable from a lane that passed. Affects every lane and kind. Fix: pass `aggregate` the lane set orchestrate resolved and report a missing expected sink as `unresolved`. (surfaced designing Q-0089, spec `specs-cr-gate-multi-reviewer-codex-headless-dispatch`)
- the codex CR lane orphans codex when the outer `execFile` cap fires. `src/cr/lanes/codex.ts` shells out through `pnpm`, and `execFile`'s timeout signals only its direct child, so the codex grandchild survives and runs to self-completion burning ChatGPT quota — unattended in drain mode. Codex-specific: `reviewer` and `verifier` dispatch through `spawnAgent` (`subagent-dispatch.ts:137`, `verify-dispatch.ts:74`), which already spawns `detached: true` and group-kills. Deliberately NOT fixed inside `spawnCodex` — three CR rounds on Q-0089 established that an inner cap drags in a kill path, `detached`, a Ctrl-C signal reaper and two out-of-process fixture harnesses, none of which the stderr-drain fix needs. Likely right shape: route this lane through `spawnAgent` like the other two rather than growing a second kill implementation. (surfaced designing Q-0089, same spec)
- remove noldor-absorb? [triaged 2026-08-10 → skill-surface-pruning-audit]
- remove noldor-new-feature? [triaged 2026-08-10 → skill-surface-pruning-audit]
- triaged věcí by v ideas zůstat měli, ale můžou spadnout dolů někam do vlastní Heading sekce [triaged 2026-08-11 → triaged-bullet-archive-section]
- architecture part for consumer -> some extra folder with file/files -> diagramming [triaged 2026-08-11 → consumer-architecture-doc-surface]
- brainstorming of things is not that great as with superpowers [triaged 2026-08-11 → spec-brainstorming-depth-parity]
- mandatory codex review -> atleast one round on bigger tasks [triaged 2026-08-11 → mandatory-codex-review-round]
- reviewer-lane dispatch timeout too tight: `subagent-dispatch.ts` hard-codes `timeoutMs: 600_000`, and a med-effort full-spec review that follows the verify-before-flag protocol (runs typecheck/tests) can exceed 10 min — three consecutive `exit -1 (timeout)` failures in one session, each burning the full window and writing a synthetic red sink. Make the timeout configurable (`crReview.dispatchTimeoutMs`?) and/or retry once with backoff; consider telling the reviewer prompt to skip long commands when the artifact is markdown-only. (surfaced shipping charuy agent-skill-bundle, charuy PR #91) [triaged 2026-08-11 → reviewer-lane-dispatch-timeout-configurable]
- codex lane broken against codex-cli 0.133.0: (a) `--base-sha` path errors and the fallback still exits 1; (b) prompt passed as positional argv makes codex print "Reading additional input from stdin..." and hang/dump a 478KB models-cache error in headless runs — prompt must go via stdin (`codex exec - <<EOF`); (c) expired ChatGPT auth surfaces as bare exit 1 in the sink with no hint. Fix: stdin dispatch, auth preflight with a clear `codex login` message in the sink, and version-probe the installed CLI. (surfaced shipping charuy agent-skill-bundle, charuy PR #91) [triaged 2026-08-11 → codex-lane-headless-dispatch-breakage]
- `sync code-links` is destructive on consumers without `@fd:` tags: scanning tagged code and writing `links.code` on every FD means an untagged consumer gets all hand-curated `links.code` arrays wiped to `[]` (charuy: 35 FDs emptied in one run, caught only by git diff). Fix: never write an empty array over a non-empty one without `--force`, or skip FDs with zero matching tags and print a per-FD `skipped (no tags, existing links kept)` line. (surfaced shipping charuy agent-skill-bundle, charuy PR #91) [triaged 2026-08-11 → sync-code-links-destructive-without-fd-tags]

### Confirmed bugs from the 2026-08-12 deep audit

- dashboard queue writes have a reproducible **lost-update race** even though the HTTP surface advertises optimistic concurrency through `If-Match`. Every mutator in `src/dashboard/api/blocks.ts` performs `readFile → sha256/If-Match check → transform → atomicWriteFile` without a critical section. Two requests can therefore read the same contents, both pass the same ETag, and both prepare different successor states. The writer makes this substantially worse: `src/dashboard/api/atomic.ts:17` always uses `<basename>.tmp.<process.pid>`, so every concurrent write in one dashboard process targets the same temporary file. Focused reproduction: launch two `handleMove` calls concurrently with the same valid ETag, repeat 50 times; all 50 trials ended as one `200` plus one thrown `ENOENT` from `rename`, and the final file retained only one user action. A unique temp suffix would stop the `ENOENT` but would **not** stop stale writers from overwriting each other; the compare and write must become one serialized operation per target repository/file, with the file re-read and ETag rechecked inside that critical section. Regression tests need a controlled barrier that makes two calls pass the initial read simultaneously, then asserts exactly one `200`, exactly one `412`, no thrown filesystem error, no leftover temp file, and no silent loss. Cross-file promote/demote must use the same mutation coordination so a single-file move cannot race a cross-section move. (confirmed by runtime probe in read-only audit 2026-08-12)
- dashboard `--docs` has contradictory path semantics and the documented invocation is broken. `README.md:34` says `pnpm noldor dashboard server --port 4321 --docs ./docs`, and the original extraction design explicitly describes the flag as a **docs-directory** override containing `roadmap.md`, `features/`, `milestones/`, etc. Current `setDocRootsOverride()` stores that value as `getDocRoot()`, but `loadDocRoots(cwd)` now treats its argument as a **repository root** and appends `docs/` (`src/core/doc-roots.ts:47-56`). The documented command consequently resolves `docs/docs/roadmap.md`; a focused call to `setDocRootsOverride('./docs')` followed by a roadmap load fails with `ENOENT`. Decide and name one contract: either keep `--docs <docs-directory>` and teach the dashboard adapter not to append `docs`, preserving the published command and historical design, or intentionally replace it with `--root <repository-root>` and migrate README/help/tests. Acceptance must exercise the packaged CLI against a scratch consumer whose repository root and docs directory have distinguishable sentinel content; parsing the flag alone is not a useful test. (confirmed by runtime probe and design-history comparison in read-only audit 2026-08-12)
- even if `--docs` is reinterpreted as a repository-root override, the dashboard currently builds **mixed-repository views** because many loaders ignore it and continue reading `process.cwd()`. `loadSddInput()` in `src/dashboard/data.ts:1124-1177` hardcodes `docs/features`, `ideas.md`, `docs/backlog.md`, design dirs, README, graph path, and scan roots; `scanRoots()` and `actualPackageNames()` also default to cwd. Git helpers execute without an override-aware `cwd`; milestone groups call `loadMilestones()` / `loadMilestoneBySlug()` with their defaults; feature-body VS Code link rewriting uses `process.cwd()`. Focused reproduction: set the dashboard root to `/private/tmp/not-the-cwd`, then call `loadSddInput()`; it still returned this checkout's 75 features, `graphify-out/graph.json`, and `src` scan root. The visible result is worse than a clean failure: counts, gaps, Git activity, milestone state, and documents can silently describe different repositories on one page. Fix direction: construct one immutable repository context/snapshot from the selected root and thread it through every loader and Git invocation; remove module-global override state. Test with two complete fixtures containing deliberately incompatible feature counts, Git histories, package layouts, and milestone names, and assert that no value from fixture A appears while rendering fixture B. (confirmed by runtime probe in read-only audit 2026-08-12)
- `noldor worktrees up` and `noldor worktrees down` accept unvalidated slugs and can escape the intended `.worktrees/` / `.noldor/` locations. `createWorktree()` correctly calls shared `isSlug`, but `upWorktree()` computes `join(opts.cwd, '.worktrees', opts.slug)` before validation and calls `createWorktree()` only when the computed path does not already exist. An existing outside directory—or `--no-create`—therefore bypasses the only guard, after which Noldor can open an editor there, launch an agent there, and boot configured commands there. `downWorktree()` similarly constructs the pid-file path and, with `--remove`, passes `join('.worktrees', opts.slug)` to `git worktree remove --force`; e.g. `../../outside` normalizes outside `.worktrees`. A sufficiently deep value in the prefixed pid filename can also escape `.noldor`, letting the command read/remove a foreign `.pids` file and interpret its second column as process-group IDs. Validate with the canonical `src/core/slug.ts` rule at the start of both exported library functions—not just CLI parsing—before any `exists`, read, process launch, kill, or Git call. Tests should inject spies and prove invalid slugs produce zero side effects for the normal, existing-path, `--no-create`, and `--remove` branches. (confirmed by static path-resolution probe in read-only audit 2026-08-12)
- `noldor features phase-flip-done` and `features phase-revert` join an unchecked positional argument into `docs/features/<slug>.md`. Both CLIs select the first non-flag token and immediately call `join(process.cwd(), 'docs', 'features', `${slug}.md`)`; neither imports the shared canonical slug validator even though `src/core/slug.ts` explicitly says every external path-building entry point must use it. With a consumer root of `/consumer`, `../../../escape` resolves to `/escape.md`; if that file exists and contains the expected phase text, the command rewrites it. This is a local path-traversal write primitive in commands routinely invoked by automated gate flows. Validate the slug before `existsSync`, keep the pure phase transformers unchanged, and return the same clear invalid-slug diagnostic used by worktree creation. Add subprocess tests for slash, `..`, leading/trailing/doubled hyphens, uppercase, and Unicode; assert no read/write spy fires and an outside sentinel file remains byte-identical. Also audit every other CLI that forms a feature path from argv so this does not become a two-call-site patch around a shared policy failure. (confirmed by static path-resolution probe in read-only audit 2026-08-12)
- milestone draft/load/activate paths have the same unchecked-slug traversal class. `src/milestones/lib.ts:55-76` and `:90-99` interpolate `slug` into `docs/milestones/<slug>.md`; the CLI forwards `rest[0]` verbatim and the library exposes the unsafe functions to non-CLI callers. `../../../escape` from a typical consumer root normalizes to an arbitrary absolute markdown path; `draft` can create a new outside file when its parent exists, while `activate` can read and rewrite an outside file if it happens to carry milestone-shaped frontmatter. `FeatureFrontmatterSchema.milestone` and vision's `current-milestone` are also only non-empty strings, so repository-authored references can feed non-slugs deeper into milestone readers. Use the shared slug schema at every trust boundary and preferably encode it in milestone/feature/vision validation so invalid state cannot be loaded at all. Acceptance: invalid CLI and direct-library inputs fail before filesystem access; feature and vision validation reject path separators and dot segments; valid historical slugs remain backward compatible. (confirmed by static path-resolution probe in read-only audit 2026-08-12)
- roadmap ↔ backlog promotion/demotion can leave queue state half-applied by design. `crossSection()` in `src/dashboard/api/blocks.ts:329-357` removes the block from the source with an atomic rename, then writes the destination. If the second write fails, the entry is already gone from its source; the route returns `500` and logs `git restore docs/roadmap.md docs/backlog.md`, which also discards unrelated concurrent/user edits and is not an acceptable transactional recovery mechanism. The combined ETag prevents some stale clients but does not make two renames atomic, and the dashboard lost-update race can interleave additional mutations between them. Introduce a recoverable multi-document transition: serialize all queue mutations, precompute and schema-validate both successor files, persist enough journal/backup information to finish or roll back after any failure, and only expose success once both documents represent the same transition. Tests need injected failures before/after each durable step, restart recovery, preservation of unrelated content, and proof that an entry is never missing from both files or duplicated in both after recovery. (confirmed by failure-path inspection in read-only audit 2026-08-12)
- the still-routable dashboard `POST /api/roadmap/add` path writes entries that immediately violate Noldor's queue invariants. `buildRoadmapBlock()` emits area, type, since, size, impact, and body but never a stable `- id:`; `handleAdd()` treats type/size/impact as optional, and `src/dashboard/__tests__/api-blocks.test.ts:407-410` explicitly blesses a bare roadmap block containing only area and since. In any consumer with `.noldor/id-counter.json`, `validate triage` requires an ID on every queue entry; roadmap validation also requires type, size, and impact. The UI add forms were removed, but `src/dashboard/server.ts:239-240` keeps the unauthenticated loopback route callable, so scripts or stale clients can corrupt queue validity. Make an explicit product decision: delete the dead route and tests, or make it use the same ID allocator, required-field schema, and strict validation as triage/promotion. Acceptance should submit the minimum accepted payload through the HTTP handler, then run `validateTriageInputs(... strict: true, counterExists: true)` on the written file and require zero errors; parallel adds must also prove ID uniqueness. (confirmed by cross-checking writer and validator contracts in read-only audit 2026-08-12)
- dashboard overview crashes on a valid/minimal consumer that has no `scripts/` directory. `loadCounts()` runs `countMatching(getSkillsDir(), ...)` and `countScriptFiles()` in parallel. The former intentionally catches missing directories and returns zero; `countScriptFiles()` at `src/dashboard/data.ts:1296-1304` calls recursive `readdir` without an ENOENT branch, causing the whole overview route to render an internal error. A temp consumer with minimal docs and no scripts reproduced the `ENOENT`. Consumer layouts are explicitly optional/configurable and templates do not guarantee a scripts tree, so absence must mean count zero, while permission/I/O errors should remain visible rather than being swallowed by a broad catch. Add a fixture with no scripts, an empty scripts dir, nested source/test scripts, and an injected `EACCES`; require 0, 0, the correct filtered count, and a surfaced error respectively. (confirmed by runtime probe in read-only audit 2026-08-12)
- milestone activation is described as atomic but performs three ordinary sequential writes and has a stale-state no-op. `activateMilestone()` preflights inputs, then writes target `status: active`, writes vision's `current-milestone`, and finally writes the prior active milestone as `shipped` (`src/milestones/lib.ts:149-167`). Failure at step two leaves an active milestone invisible to vision; failure at step three leaves two active milestones. No temp/rename, journal, rollback, or post-write validation exists. Separately, if the target is already `active`, line 155 returns before ensuring vision points to it; `noldor milestone activate foo` can print success while leaving the dashboard with no current milestone. Model activation as a pure transition plan plus one recoverable multi-document application owned by the repository mutation module; idempotent re-activation should repair all derived state. Fault-injection tests must fail each durable step, reopen the repository as a fresh process would, run recovery/validation, and assert exactly one active file with vision pointing to it. (confirmed by code inspection and temp-consumer probe in read-only audit 2026-08-12)
- the milestone YAML scalar writer generates files that its own schema cannot read for valid-looking input. `yamlScalar()` quotes only characters such as `:`, `#`, braces, brackets, and quotes, but YAML implicit scalars also include booleans/null/numbers and multiline values. Reproduction in a temp consumer: `draftMilestone('true', 'false')` writes `name: true` and `description: false`; `loadMilestones()` then reports both fields as booleans where `milestoneFrontmatterSchema` requires strings. A shell argument containing a real newline can also inject malformed or additional frontmatter because newline is not quoted/normalized. Stop hand-serializing YAML: use the same mature serializer/parser policy used for other frontmatter, or implement a scalar encoder proven against the full YAML 1.2 implicit-type set and control characters. Regression matrix should include `true`, `false`, `null`, numeric-looking text, dates, leading `-`, `#`, colons, quotes, backslashes, and newlines, and must round-trip exact strings through `draft → read → validate`. (confirmed by runtime probe in read-only audit 2026-08-12)
- `sync doc-links` and `sync test-links` cannot clear a feature's **last removed tag**, leaving stale `links.docs` / `links.tests` forever. Both scanners build a map containing only slugs found in the fresh scan and then update only `for (const [slug, paths] of map)`. When some tagged files remain, removed paths disappear correctly; when the last tag is removed or the last tagged file is deleted, the slug disappears from the map and its cached frontmatter array is never visited. `sync code-links` already documents and implements the correct scanned∪cached iteration plus explicit empty-projection policy, showing the intended shape. Bring docs/tests onto a shared projection engine that loads cached arrays, diffs the union of slugs, distinguishes deliberate clearing from scan failure, and offers `--check` parity. Tests must start with one cached path, remove the final source tag/file, run sync, assert the array becomes empty, and prove a missing/unreadable scan root does not masquerade as authoritative emptiness. (confirmed by control-flow comparison in read-only audit 2026-08-12)
- the live docs gate is already red: a fresh `pnpm noldor docs check` reports 15 markdown files with broken internal links. The failures are mostly path-ownership drift after code moves (`src/features/feature-schema.ts` → `src/core/feature-schema.ts`, `src/cr/config.ts` / `review-profile.ts` → core), removed garden files, design artifacts moved to archive without inbound-link rewrites, a missing `.claude/CLAUDE.md`, one bad skill-catalog anchor, a missing generated how-to index, and `docs/release-notes.md:117` walking one directory too far to `src/core/session.ts`. This contradicts the repo's completion gate even though build/typecheck/lint and all tests pass. Repair current links through the appropriate doc-gardening/migration flow rather than blanket string replacement, then add relocation tests or link-rewrite tooling for source-module moves and design archival. Acceptance is a clean `pnpm noldor docs check` from a fresh checkout plus targeted assertions that the relocated targets resolve. Do not “fix” historical links by creating fake compatibility files. (confirmed by fresh docs-check run in read-only audit 2026-08-12)
- `docs check` has an undocumented **design-doc blind spot**: `src/docs/docs-check.ts:8` puts the generic directory name `design` in `EXCLUDED_DIRS`, so walking `docs/` skips both active `docs/design/specs/` / `plans/` and their archives. The script catalog nevertheless claims the input is the whole `docs/` tree and the output means no dangling links. A direct check of the 11 active (non-archive) design documents found 13 broken links in `2026-07-15-registry-distribution-for-the-noldor-package-public-npm-cutover-design.md`; the normal command stays silent about them. Historical archives may intentionally preserve old locations, so simply removing `design` could create 195 noisy findings across 32 archived files. Make inclusion policy path-specific: validate active specs/plans by default, decide and document whether archives are exempt or reported separately, and never use a basename exclusion that can suppress an unrelated nested `design/`. Tests need one broken active spec (red), one archive fixture with the chosen policy, and proof that another `design` directory is not accidentally skipped. (confirmed by direct checker invocation in read-only audit 2026-08-12)
- queue parsing and docs-link checking implement an incomplete fenced-code grammar: they recognize triple backticks but not CommonMark/GFM tilde fences (or varying fence lengths). Focused probe: a roadmap containing `~~~md`, then `### Phantom` + `- area: tooling`, then `~~~`, followed by a real entry is parsed as `['Phantom', 'Real']`; a markdown link inside the same tilde fence is extracted as a live internal link by `docs-check`. This can fabricate queue entries/dependencies, make writers remove or reorder example text, and produce false broken-link failures. Several independent implementations toggle on `line.startsWith('```')` or strip with a triple-backtick regex (`parseRoadmap`, `parseEntries`, `pushEmptyGroupIssues`, `stripCodeRegions`), so patching one leaves semantic drift. Establish one fence scanner that understands marker character, opening length, up-to-three-space indentation, info strings, and a closing fence of sufficient length, then reuse it across queue grammar and docs checks. Add paired fixtures for backticks/tildes, 3/4-character fences, embedded shorter runs, indented fences, and unclosed fences. (confirmed by pure-function runtime probe in read-only audit 2026-08-12)
- the published package does not encode its documented runtime floor. README and adoption guide require Node ≥20 and pnpm ≥9, and runtime source uses modern methods such as `toSorted`, but `package.json` has no `engines` field; it only has `packageManager: pnpm@9.7.1`, which does not provide the same install-time compatibility warning. Consumers on older Node can install successfully and fail later inside a gate, hook, or release command. Add precise `engines.node` and, if intentionally enforced/supported, `engines.pnpm`; ensure CI tests the minimum version rather than only a newer development version. Decide whether `engine-strict` belongs in templates or whether warnings are preferable for adoption compatibility. Acceptance: packed-package metadata contains the declared floor, installation under the minimum supported Node succeeds, and a below-floor fixture produces the intended deterministic warning/failure before executing Noldor. (confirmed by package/README contract comparison in read-only audit 2026-08-12)
- roadmap dependency metadata is already stale: fresh `pnpm noldor validate triage` exits 0 with an advisory that “Mandatory Codex Review Round” has `blocked-by: Q-0089`, but no roadmap/backlog entry ID, feature slug, or feature `entry-id` resolves that reference. This may be a typo, a shipped feature whose ID was not carried into its FD, or a retired queue entry that lost its durable identity; any case makes the dependency graph and “blocked” status untrustworthy. Resolve the intended dependency from Git/feature history, then either repair the ref or restore the missing `entry-id` through the sanctioned triage/feature flow. Add a queue-health expectation that self-host has zero unknown refs even if adoption-safe consumer validation keeps the rule advisory by default; strict CI on this repository should catch future dangling IDs. (confirmed by fresh triage validation in read-only audit 2026-08-12)
- npm packaging ships two full runtime representations plus development-only source tests/fixtures. `package.json.files` includes both `dist` and `src`; `bin/noldor.mjs` registers `tsx/esm/api` and executes `src/cli/index.ts`, so compiled `dist` is not the active CLI runtime, while the source inclusion also captures test suites and fixtures. Measured with `npm pack --dry-run --json --ignore-scripts` using an isolated cache: 2,531 entries, 2.12 MB compressed, 11.26 MB unpacked, including both trees. This is not automatically a bug—the archived package-lift design deliberately chose source-at-runtime, and current compiled manifest strings still reference `.ts` paths—but it is measurable install/bandwidth/attack-surface waste and creates two representations that can drift. Revisit the distribution decision explicitly: either publish a pruned source runtime (exclude tests/fixtures and consider dropping dist) or make dist truly executable (rewrite manifest resolution and drop runtime tsx/src). Acceptance should inspect the actual tarball, run the packed binary in a clean consumer, and assert an allowlisted file inventory/size ceiling. (confirmed by package dry-run in read-only audit 2026-08-12)

### Architecture and refactor candidates from the 2026-08-12 deep audit

- deepen a **repository mutation module** that owns every durable read-modify-write transition instead of leaving “atomicity” as a temp-file helper at each call site. Current shallow implementations are scattered across dashboard queue routes, core state writers, CR JSON writers, milestone activation, and roadmap/backlog movement; they disagree on temp-name uniqueness, serialization, schema checks, multi-file recovery, and concurrency. The module's responsibility should be: acquire repository/file-scoped serialization, re-read and compare expected state inside the critical section, compute the transition without side effects, validate successor invariants, persist with unique temp files, and recover/roll back multi-document transitions after interruption. Adapters can retain domain-specific transforms for queues, milestones, sessions, and review sinks. **Leverage:** fixes the confirmed dashboard race, partial promote/demote, milestone split-brain, and fixed-pid temp collisions through one seam. **Deletion test:** callers should lose their own ETag choreography, temp naming, sequential multi-file write blocks, and manual `git restore` recovery hints. **Risk:** a global lock would be shallow and unnecessarily serialize independent repositories/files; scope locking and crash recovery deliberately. Start with queue/milestone mutations where correctness benefit is measurable, then migrate state files only when semantics match. (architecture candidate; Strong recommendation from read-only audit 2026-08-12)
- deepen an immutable **repository context and per-request snapshot module** for dashboard and report readers. Today `dashboard/data.ts` is 2,533 LOC and mixes module-global override state, document paths, cwd-relative scanners, Git subprocesses, graph/package discovery, parsing, and page-specific aggregation. `handleOverview` concurrently invokes loaders that reload features and rescan repository state multiple times, so one response can be slow and internally inconsistent even without `--docs`; the recent Git timeout increase after hot zones blanked under load is a symptom. The deep module should anchor every adapter to one resolved root and expose a coherent snapshot of docs, configuration, scan roots, Git identity/history, and traceability inputs for a request/report run. **Leverage:** fixes mixed-root results, makes two-dashboard execution truthful, reduces duplicate I/O/Git processes, and gives tests deterministic injected snapshots. **Deletion test:** remove `docRootsOverride`, dashboard `process.cwd()` literals, default-cwd milestone/Git calls, repeated `loadFeatures()` within one page, and dashboard-specific reconstruction of SDD input. Preserve streaming/refresh behavior by setting an explicit snapshot lifetime rather than adding an unbounded global cache. (architecture candidate; Strong recommendation from read-only audit 2026-08-12)
- deepen a **review-run lifecycle module** so expected work, process ownership, bounded output, sink persistence, and aggregation share one source of truth. The five detailed Codex/aggregate bullets already above are symptoms of one shallow lifecycle: orchestrate resolves lanes but aggregate later rediscovers only existing filenames; the Codex lane shells through pnpm and owns neither the grandchild process group nor a bounded stream; code/spec/plan dispatch mode is inferred in a lossy ternary; feature capability is guessed by shelling out to intercepted help. Record a run manifest before dispatch with expected lanes/kind/artifact/base, let one process owner handle timeout/signal/group cleanup and capped diagnostics, write every terminal outcome to an expected sink, and aggregate against the manifest—not directory contents. **Leverage:** missing lanes become explicit red, timeouts cannot burn quota invisibly, code cannot fall into plan heuristics, and delta review stops depending on help prose. **Deletion test:** remove filename discovery as the expected-set oracle, `codexSupportsBaseSha`, nested pnpm timeout ownership, per-lane error-shape normalization, and duplicated process-kill implementations. Maintain lane adapters only for prompt/result semantics. (architecture candidate; Strong recommendation from read-only audit 2026-08-12)
- deepen a **traceability projection module** shared by `sync code-links`, `sync test-links`, `sync doc-links`, and `sync spec-links`. Clone detection measured large repeated groups (roughly 223/216-token sync clones) across tag extraction, filesystem walking, slug grouping, feature-frontmatter loading, array comparison, writing, warnings, and CLI summaries, yet behavior already diverges: code sync handles scanned∪cached and guarded empty scans while docs/tests cannot clear a final stale tag and lack equivalent `--check` behavior. Define one projection implementation around source adapters (tag syntax, eligible paths, destination `links.*` key) and one policy for authoritative empty scans, cached-only slugs, unknown feature tags, deterministic sorting, dry/check/write modes, and atomic validated frontmatter writes. **Leverage:** a fix to deletion, scan failure, or reporting applies to every traceability kind. **Deletion test:** remove three/four grouping loops, updateFeatureMd copies, independent walker exclusion sets, and inconsistent main/report code while keeping small adapters readable. Do not over-generalize code's special directory-entry preservation into every kind; make such differences explicit strategy data. (architecture candidate; Worth exploring from read-only audit 2026-08-12)
- deepen a **queue-document grammar module** that parses and serializes schema-C markdown once, with source ranges and complete fence awareness. Queue semantics are currently spread across `utils/parse-blocks.ts`, `utils/write-blocks.ts`, dashboard block scanners/writers, triage's `pushEmptyGroupIssues`, next-priority, and route-specific description sanitization. Each implementation recognizes a slightly different subset of headings, fences, fields, categories, and duplicates; this is why tilde fences fabricate entries and why writers must guess which markdown strings are structurally dangerous. A deep grammar implementation should own entry/group recognition, canonical field vocabulary, stable IDs/dependencies, exact source spans, comments/body preservation, and serialization; read adapters can project roadmap/backlog models and write adapters can perform range-based insert/move/remove against the same parse. **Leverage:** prevents parser/writer disagreement and lets every mutation run the same strict invariant before persistence. **Deletion test:** remove duplicate fence toggles, field regex construction, heading split/scans, countEntries/scanBlocks structure guesses, and most route sanitization. Preserve untouched formatting outside modified ranges—full-document pretty-printing would create noisy queue diffs and lower locality. (architecture candidate; Worth exploring from read-only audit 2026-08-12)
- deepen executable **CLI command definitions** and centralize trusted-input adapters. `src/cli/manifest.ts` currently knows dispatch targets and one-line descriptions; individual handlers separately own detailed usage, flags, defaults, capabilities, and validation. The dispatcher intercepts subcommand `--help`, making handler USAGE unreachable and breaking the base-SHA probe; ad-hoc argv loops silently differ; several path-building commands omit the shared slug guard. Move enough metadata/parse policy into a single command definition so help, dispatch, capability introspection, and validation cannot drift, while keeping implementation modules behind a stable seam. All external slug-shaped values should pass the same canonical adapter before any I/O, regardless of whether the caller is the packaged CLI or a library consumer. **Leverage:** closes phase/worktree/milestone traversal together, makes detailed help trustworthy, and eliminates version/capability grep hacks. **Deletion test:** remove handler-local USAGE copies, copied slug regexes, `process.argv.find` loops, and self-shelling help probes. Avoid a framework-heavy parser dependency unless the current manifest can generate the required behavior with less code. (architecture candidate; Worth exploring from read-only audit 2026-08-12)
- deepen a **design-artifact detector module** for stale specs/plans rather than maintaining mirrored loops inside `garden-detect.ts`. Clone measurement found the largest repository clone wholly inside that file: 388 tokens / 107 lines between stale-plan and stale-spec detection. Both implementations enumerate dated markdown, derive a slug, resolve ownership by feature filename, then `links.*`, then graph adjacency, then apply feature-phase/age policy and emit an archive finding; their differences are artifact kind, path resolver, link relation, and wording. Represent those differences as small adapters around one ownership/age implementation. **Leverage:** archive-policy fixes and graph fallback changes stay identical for specs and plans; adding another design-artifact kind becomes deliberate rather than copied. **Deletion test:** replace both 100+-line functions with one loop and two shallow strategies, while retaining specific exported result types if callers benefit. Tests should run the same behavioral matrix against both kinds (live owner, done owner, attach-name link owner, graph-only owner, stale orphan, recent orphan, missing directory). (architecture candidate; Worth exploring from read-only audit 2026-08-12)
- revisit the **single package runtime representation** decision as an explicit ADR/spec, not an opportunistic cleanup. Current source-at-runtime via tsx is documented historical intent, but publishing both source and dist plus tests has a measured 11.26 MB / 2,531-entry cost, and compiled `dist/cli/index.js` cannot simply replace the bin because manifest path strings still end in `.ts`. Compare three approaches with packed-consumer smoke tests: (1) keep TypeScript runtime but publish only runtime source/templates/bin and exclude tests/fixtures/dist; (2) make dist canonical, rewrite manifest/module resolution for `.js`, remove tsx/source from the package; (3) retain both only if a real supported import/debug workflow requires them and enforce build/source parity. Recommendation to explore (1) first because it preserves behavior with the smallest migration, then quantify cold-start/install/security differences before considering (2). **Deletion test:** one complete runtime tree and one module-resolution policy remain in the tarball. **Risk:** package-lift chose the current shape for portability; any change needs clean `npm pack` install/CLI/hook/dashboard tests and a compatibility statement for consumers importing undocumented source paths. (architecture candidate; Speculative because it conflicts with archived design, read-only audit 2026-08-12)
- add a current **domain/module map plus decision-record surface**. The repository has rich feature docs and 107 design artifacts, but no root `CONTEXT.md`, module map, or `docs/adr/`; maintainers and agents must infer current architecture and rationale from 47k runtime LOC plus historical specs, some of whose links are already stale. This makes unusual but intentional constraints—source-at-runtime packaging, adoption-safe advisories, sequential queue writes, graph fallbacks—look like accidental bugs, while actual cross-module seams such as repository mutation and snapshot ownership remain implicit. Create a concise current map using the project's domain vocabulary, showing major modules, dependency direction, durable state, entry points, and where each decision record lives; add ADRs only for active consequential choices rather than backfilling history. **Leverage:** improves locality for reviews/refactors and gives future architecture audits a decision baseline. **Deletion test:** new readers should not need to traverse archived plans to answer “which module owns repository paths/writes/review completion?” This overlaps the already-triaged `consumer-architecture-doc-surface`; either broaden that item explicitly or keep framework-internal architecture as a separate independently promotable idea. (architecture candidate; Worth exploring from read-only audit 2026-08-12)

## Lessons

Raw capture point for operator/agent lessons + gotchas. `/noldor-absorb` classifies each unfiled bullet (`drop | gotcha | actionable | feedback`), files it into framework docs, and stamps `[absorbed YYYY-MM-DD → <dest>]`. Stamped bullets may be pruned — git history is the audit trail.

- `pnpm noldor worktrees create` writes an untracked `.env.local` (`PORT=<assigned>`) that is not in `.gitignore`, so every fresh worktree starts with a dirty tree: `ensureCleanTree` counts `??` entries, so `pr-flow` preflight refuses to ship until the operator deletes a file the framework itself created. Same shape as the known `.claude/settings.local.json` verify-lane fmt killer. Fix: add `.env.local` to `.gitignore` (self-host + `templates/`), or have `worktrees create` write it only under an already-ignored path. (surfaced shipping Q-0073, PR #268) [absorbed 2026-08-10 → ideas]
- repeated `cr orchestrate --kind code` runs across amend rounds APPEND a `Noldor-Reviewed-Subagent` trailer instead of replacing the existing one, so a commit that went through N CR rounds carries N receipts — all but the last stale, since each amend changes `HEAD^{tree}`. The pre-push hook validates against the tree, so a stale receipt is at best noise and at worst a false pass if the wrong one is read. Manual cleanup today: `git log -1 --format=%B | grep -v '^Noldor-Reviewed-Subagent:' > msg && git commit --amend --file=msg`, then re-run orchestrate. Fix: make the receipt amend replace any existing trailer of the same key. (surfaced shipping Q-0073, PR #268 — 14 CR rounds, 2 receipts accumulated) [absorbed 2026-08-10 → ideas]
- design lesson from 14 CR rounds on Q-0073: the first cut of finish-mode carried a `finishable` Set that had to be mutated at every ship / skip / merge / retry / timeout leaf, and rounds 5, 10, 11 and 13 each found a different missed `delete`. Replacing it with a verdict recomputed fresh immediately before each spawn (`resolveFinishPrompt`) made the whole class of finding vanish and left all 215 autonomous tests passing unchanged. Prefer a recomputed decision over maintained state whenever the state has many mutation sites — and read four rounds of "you missed another unwind" as the reviewer circling a design smell, not as four separate bugs. (surfaced shipping Q-0073, PR #268) [absorbed 2026-08-10 → workflow]
- the worktree edit-path trap has a **false-green** failure mode nobody has written down: when a gate session edits via main-workspace-absolute paths instead of worktree-absolute ones, the edits land on `main` while `pnpm typecheck` / `pnpm test` still run inside the (unchanged) worktree — so both pass, and the pass proves nothing about the change. `git status` in the worktree is the only cheap tell (clean when it should be dirty). Recovery is lossless: `git -C <main> diff -- src/ > p && git -C <worktree> apply --3way p && git -C <main> checkout -- src/`, then re-verify; compare test COUNTS before/after (97 → 111 here) since a suite that never loaded the new tests still reports green. Fix candidates: have the gate echo the worktree root as the edit-path prefix at scaffold time, or add a pre-commit/`noldor verify` assertion that the worktree tree is non-clean before a fast-track commit. (surfaced shipping Q-0088, PR #290)
- `cr orchestrate` and `pr-flow` both amend/commit, and every backgrounded git invocation in this session left the Bash CWD silently back at the main workspace — so a later bare `cat .noldor/cr/<slug>-code-reviewer.json` read the WRONG repo and a bare `git log -1` showed a previous feature's commit (`sdd-report-quote-normalization`), which reads exactly like a lost receipt. Prefer `git -C <worktree>` / absolute paths for every post-commit check in a worktree session rather than trusting CWD persistence. (surfaced shipping Q-0088, PR #290 — already noted for Q-0077/#264, recurred anyway)
- the diff-scoped clone verdict fires on any line you EDIT inside a pre-existing clone, not only on a copy you just wrote — which is a false positive against its own stated intent ("did you just write a copy of something that already exists?"). Concretely: a one-line `desc:` string edit in `src/cli/manifest.ts` flagged two 72/98-token groups, because the manifest is a data table of near-identical `name: { desc, subs: { '': { src, desc } } }` entries and the pre-existing structural clone contains the touched line. There is no ignore knob and no per-finding override, so the only ways past are to abandon the edit or refactor ~40 unrelated table entries. Root cause located: `flaggedGroups` (`src/clones/diff-scope.ts:187`) reds when ANY single instance overlaps a changed line at all, so a three-line graze scores the same as writing the whole copy. Note the tempting fix is wrong — requiring ≥2 overlapping instances breaks the primary case, because pasting an existing block into a new file changes exactly one instance; the predicate has to be coverage-based instead. Consequence for Q-0094: the `clones` manifest `desc` still reads `<report|check>` and omits the new `baseline` sub — `noldor help` is stale. Filed as Q-0095 after it fired twice more while shipping author-side-rule-injection (added imports landing in a matching import block; a new function's tail abutting the lane sink-path prologue), that PR having to push with `LEFTHOOK_EXCLUDE=noldor-clones`. (surfaced shipping Q-0094, confirmed Q-0095)
- `clones check` cannot see an UNTRACKED new file: `git diff` has no post-image for one, so the diff-scoped verdict reports `no clone group touches this change - green` for a file whose every line is new. The gate only starts reviewing a new file once it is committed — so a pre-commit "green" proves nothing, and the findings arrive at pre-push instead, after the CR receipt is already stamped. The clones-cli test suite documents the mechanism in a fixture comment but nothing warns the operator. Fix: have `resolveChangedRanges` union `git diff` hunks with the full line span of every untracked corpus file (or at least print "N untracked file(s) not reviewed" so the green is qualified). (surfaced shipping Q-0094)
- the token-level detector treats a new zod schema as a clone of every existing one: `src/clones/baseline.ts` matched `src/dashboard/data.ts` at 163 tokens and `src/cr/autofix-ledger.ts` at 71 tokens purely because Type-2 normalization makes `foo: z.number().int().nonnegative(),` identical to `bar: z.number().int().nonnegative(),` — five such fields in a row clears the 50-token floor on their own. The honest local fix is to name the repeated validator once (`const measured = z.number().int().nonnegative()`), which really did cut whole-corpus duplication by 307 tokens, so the signal is not worthless — but any schema-heavy file will keep tripping this, and consumers will read it as noise. Worth considering a tokenizer rule that collapses a chained-builder call sequence to one token, or a per-file exemption for declaration-only modules. (surfaced shipping Q-0094)

## Verticals

### Tooling

#### Now

- port ponytail's core (pre-generation "lazy senior dev" decision ladder) into the rules cascade as `.noldor/rules/lazy-decision-ladder.md` + `noldor:cut` marker wired into the CR simplification dimension [triaged 2026-08-10 → lazy-decision-ladder]

#### Next

#### Later

- `pnpm noldor worktrees create` writes an untracked `.env.local` (`PORT=<assigned>`) that is not in `.gitignore`, so every fresh worktree starts with a dirty tree: `ensureCleanTree` counts `??` entries, so `pr-flow` preflight refuses to ship until the operator deletes a file the framework itself created. Fix: add `.env.local` to `.gitignore` (self-host + `templates/`), or have `worktrees create` write it only under an already-ignored path. (surfaced shipping Q-0073, PR #268) [triaged 2026-08-10 → worktree-env-local-not-ignored]
- repeated `cr orchestrate --kind code` runs across amend rounds APPEND a second review-receipt trailer instead of replacing the existing one — the key is `Noldor-Reviewed-Subagent`, and a commit that went through N CR rounds carries N receipts, all but the last stale (each amend changes `HEAD^{tree}`); pre-push validates against the tree, so a stale receipt is noise at best, false pass at worst. Fix: receipt amend should replace any existing receipt of the same key. Manual cleanup meanwhile: `git log -1 --format=%B | grep -v '^Noldor-Reviewed-Subagent:' > msg && git commit --amend --file=msg`, then re-run orchestrate. (surfaced shipping Q-0073, PR #268) [triaged 2026-08-10 → cr-receipt-amend-must-replace-same-key-trailer]
- connect milestones to roadmap/backlog tasks: milestones (`docs/milestones/<slug>.md`) currently live independent of the queue — no way to say which roadmap/backlog entries belong to which milestone, or see milestone progress from the queue side. Idea: link entries to a milestone (e.g. `milestone:` field in schema-C blocks or milestone doc lists entry IDs) so dashboard/status can roll up milestone completion from its tasks. [triaged 2026-08-10 → milestone-queue-linking]
- cr-autofix polish residue from the Q-0075 ship (PR #276, CR rounds 9–16, overridden at round 16 with the sole med blocker fixed): (a) `DecideResult.baseSha` doc overstates its invariant — it is empty on ANY decline with git unreachable, not only `no-base-sha`; say "non-empty whenever verdict is auto-fix". (b) `no-base-sha` fires before `next` is known, so a MIXED round with git unreachable declines to operator even though `apply-then-stop` never needs a base-sha — forfeits an applicable mechanical subset on an unrelated failure. (c) `round: 3/2` prints on the round-cap decline (`priorRounds.length + 1` unconditionally) — clamp or relabel. (d) `prior-deferred` scanning every round leaves the seam dead for the rest of the session after one MIXED round, including the operator's own full-review follow-up where the laundering path cannot occur; the only reset is session end — deliberate, but say so in the docs or narrow it. (e) drain-mode/SKILL twins mention `record` without naming the exit-2 `--deferred` cross-check. All in `src/cr/autofix.ts` / `autofix-cli.ts`. (surfaced code-stage CR of Q-0075, PR #276) [triaged 2026-08-10 → cr-autofix-polish-residue]
- `doctor`'s framework-skew check compares the anchor by string `!==` (`src/cli/commands/doctor.ts:63`), so an anchor _ahead_ of the installed version prints `run 'noldor upgrade'` forever while `upgrade` correctly refuses to rewrite it backwards — an advisory dead end with no CLI exit, the same shape as Q-0076 in the opposite direction. Reachable after a downgrade (`pnpm add @david.zoufaly/noldor@<older>`) or a hand-edited anchor. Fix: compare with `semver.lt(anchored, installed)` and give the ahead case its own message (`anchored <a> is ahead of installed <i> — the install is behind, not the anchor`) rather than pointing at a command that cannot help. (surfaced in the code-stage CR of `upgrade-never-advances-a-stale-anchor-on-an-empty-migration-chain`, PR #270) [triaged 2026-08-10 → doctor-ahead-anchor-dead-end]
- `noldor upgrade` writes the framework anchor on the empty-chain path before the dirty-tree guard (`src/cli/commands/upgrade.ts:90` vs the `isDirty` check below it), so a dirty tree silently gets `.noldor/config.json` mutated while the non-empty-chain path refuses with `refusing to upgrade on a dirty git tree`. Pre-existing for the bootstrap case, broadened to the stale-advance case by Q-0076. Deliberately left as-is: hoisting `isDirty` above the block would make a pure `nothing to do` invocation throw on any dirty tree, and gating only the write would re-strand the very consumer Q-0076 unstrands. Decide whether the asymmetry is intended and say so in the code, or gate the write with a narrower guard. (surfaced in the code-stage CR of Q-0076, PR #270) [triaged 2026-08-10 → upgrade-empty-chain-dirty-tree-guard]
- `sdd:report` quotes untriaged idea bullets verbatim into `docs/sdd-report.md`, so idea PROSE can redden two live-tree tests in `src/garden/__tests__/sdd-report.test.ts`: non-oxfmt markdown in a bullet (e.g. star-italics) fails the "writes oxfmt-compliant markdown" test, and a bullet naming the review-receipt key followed later on the line by the word "trailer" trips the omit-gate-compliance regex. Harden the seam: fmt-normalize quoted idea text in the report generator, and scope the test's negative assertion to the Gate-compliance section heading instead of a whole-document regex. (surfaced pre-release sweep 2026-08-10 — two ideas.md bullets moved into `#### Later` by PR #279 broke `pnpm verify` on main) [triaged 2026-08-10 → sdd-report-quote-normalization]

#### Now

- better unit tests rules on top of https://github.com/gooddata/gdc-mastercard-panther/pull/2542 [triaged 2026-08-05 → better-unit-test-rules]

#### Next

#### Later
