# Ideas

Raw entry point for human-generated ideas. `/triage` promotes bullets into `docs/roadmap.md` (flat priority-ordered list) or `docs/backlog.md` (parking lot).

- 3 verticals: tooling, business, core product
- roadmap: flat priority-ordered list (file order = priority); every entry is a `### <Entry Name>` heading at one fixed level — never a `### <Category>` container (`validate:triage` errors on `empty-group-heading`)

## Notes

## Priority

## Not groomed

## Lessons

Raw capture point for operator/agent lessons + gotchas. `/noldor-absorb` classifies each unfiled bullet (`drop | gotcha | actionable | feedback`), files it into framework docs, and stamps `[absorbed YYYY-MM-DD → <dest>]`. Stamped bullets may be pruned — git history is the audit trail.

- `/noldor-gate` Step 4 prescribes `--lanes reviewer` for the code-stage CR, but `.noldor/config.json` sets `crLanes.code: ['reviewer', 'verifier']`. Passing `--lanes reviewer` explicitly silently under-runs the repo's own configured review posture — the verifier lane never runs and the aggregate still reads green. The skill should tell the controller to prefer `--autonomous` (which reads `crLanes.code`) over the hardcoded `--lanes reviewer` example, or at minimum to check the config first. (found 2026-08-24 shipping Q-0158) [absorbed 2026-08-25 → cr-pipeline]
- Re-running `cr orchestrate` over an existing sink without `--autonomous` fires `guardLaneOverwrite`'s interactive prompt, which dies instantly with `ExitPromptError: User force closed the prompt` in any non-TTY runner (the Claude Code Bash tool included) — indistinguishable from a real failure. The delta-re-earn recipe in Step 4 shows the command without `--autonomous`, so it is a trap on the second pass by construction. (found 2026-08-24 shipping Q-0158) [absorbed 2026-08-25 → cr-pipeline]
- A commit that touches `src/**` *and* `docs/noldor/**` needs a `Noldor-Sibling-Scope: noldor:<page>` trailer, and the `noldor-scope` hook only says so after the commit is rejected. Worth pre-empting in the gate prose for any change whose fix spans code plus its runner-neutral doc twin. (found 2026-08-24 shipping Q-0158) [absorbed 2026-08-25 → ideas]
- `gh pr merge --auto` is rejected on this repo (`Auto merge is not allowed for this repository (enablePullRequestAutoMerge)`); `pr-flow` falls back to a direct squash-merge and ships fine. The fallback is silent-by-design, but the repo setting means the `--auto` path is dead code here — either enable auto-merge in repo settings or stop attempting it first. [absorbed 2026-08-25 → pr-flow]
- CR round arithmetic: the bounded re-round cap (2) governs *arbitration* rounds, but the `Noldor-Reviewed-Subagent` receipt still has to be earned by a green reviewer run over the final tree. At the cap with a red round, those two rules pull opposite ways and the skill does not say which wins. Q-0158 needed a 4th dispatch purely to re-earn the receipt after the cap-round fix. (found 2026-08-24 shipping Q-0158) [absorbed 2026-08-25 → cr-pipeline]
- The clones ratchet and the clones *group* check disagree on attribution: `clones check` printed `no clone group touches this change - green` while the same run reddened on `duplicated tokens rose 27735 -> 27845 (+110)`. The rise came from a new test reusing its file's established 15-instance scaffold (`const commits: Commit[] = [...]` + `checkCrGate({...runGit: makeGitFake(commits)})`), i.e. exactly the idiom consistency the test rules ask for. So any PR that adds a test to a table-driven test file owes a baseline re-record commit, and the check itself declines to name the offending lines — the operator has to diff group lists between branches to find them. Either the ratchet should exempt `**/__tests__/**` (or weight it separately), or `clones check` should name which files moved the total the way `microChoreOffenders` names its offenders. Related to Q-0165 (preflight/hook disagreement) but distinct: this is the standalone CLI disagreeing with itself in one run. (found 2026-08-25 shipping Q-0164) [absorbed 2026-08-25 → ideas]

## Verticals

### Tooling

#### Now

#### Next

#### Later

#### Now

#### Next

#### Later

## Triaged

- architecture -> hodně ukecaná -> přidat do frameworku i formu textu -> strukturu dokumentu -> víc 4C -> víc technické, k věci [triaged 2026-08-24 → architecture-doc-prose-form-and-structure]
- provázat milestone s featurama -> backlogem + roadmapou [triaged 2026-08-24 → milestone-queue-linking]
- ui module should use vscode of pen.dev by default then desktop app -> agent can find a .pen file and open it or create a new one if needed [triaged 2026-08-25 → pencil-bridge-editor-default-and-auto-open]
- noldor id-counter hands out live IDs: `.noldor/id-counter.json` sat at `next: 157` while `Q-0157` was already consumed (`.noldor/retired-entry-ids.json` → `panel-clipped-popovers-portal-rewrite`), so `pnpm noldor triage mint-id` minted a colliding ID. `Q-0156` is likewise claimed by both this FD's `entry-id` and a retired `sample-scene-gallery-tailwind-rewrite`. The counter is not advanced by the retirement path, so mint-id can silently duplicate. Had to mint, check against roadmap+backlog+FDs+retired-map by hand, and re-mint. Counter should be `max(existing)+1`, or retirement should bump it. (surfaced in charuy by the liquid-glass-ui ship, 2026-08-25) [triaged 2026-08-25 → stable-entry-ids-for-roadmap-backlog]
- `pnpm noldor design archive` repoints `links.design` to the archive path but leaves `links.spec` pointing at the pre-archive location, so every FD it touches ends the session with a broken spec link. Caught by the code-stage CR, not by `features validate`. Should repoint every `links.*` it moves — or `features validate` should resolve them. (surfaced in charuy by the liquid-glass-ui ship, 2026-08-25) [triaged 2026-08-25 → design-archive-leaves-the-spec-link-stale]
- Code-stage CR does not converge on a large diff: ~18 `cr orchestrate --kind code` rounds on liquid-glass-ui, each returning exactly one new finding, never green — so the `Noldor-Reviewed-Subagent` receipt is never earned and `pr-flow` cannot push without an override. The findings were real (a typecheck break, three shipped sub-AA regressions, several fail-open holes in a guard), so this is not reviewer noise; the loop simply has no fixed point, because each fix is fresh surface. The gate's own bounded re-round rule caps operator rounds at 2, but nothing reconciles that cap with the receipt requirement: obeying the cap means never shipping. Needs a rule for what earns a receipt when review is genuinely unbounded. (surfaced in charuy by the liquid-glass-ui ship, 2026-08-25) [triaged 2026-08-25 → cr-re-round-cap-enforcement-and-oscillation-detector]
- `pnpm noldor sync code-links` is repo-wide with no scope flag: run inside a feature worktree to populate one FD's links, it also rewrote `coordinate-frame-and-measurement-tools.md` (reordered + added 6 entries), pulling unrelated FD churn into a feature PR. Had to revert it by hand. Want `--slug <slug>` to scope the write. (surfaced in charuy by the liquid-glass-ui ship, 2026-08-25) [triaged 2026-08-25 → scoped-link-sync-for-the-projection-runners]
- `pnpm noldor commit` was SIGKILLed (exit 137) on a commit with a long multi-paragraph `-m` body; plain `git commit -F <file>` with the identical message succeeded and all hooks ran green. Looks like the wrapper OOMs or is killed on large message input — worth reproducing, since the wrapper is the documented path and its failure is silent (no output at all before the kill). (surfaced in charuy by the liquid-glass-ui ship, 2026-08-25) [triaged 2026-08-25 → noldor-commit-sigkilled-on-a-long-message-body]
- https://github.com/davidzoufaly/noldor/pull/372 has so many duplicates of the same text -> drift risk -> what to do with it? text imports? [triaged 2026-08-25 → doc-text-duplication-and-text-imports]
- Neither UI-design review lane is enabled anywhere, four days after the second one shipped. noldor declares no `consumer.uiPaths`; charuy declares `uiPaths` but no `uiSurfaces`, no `uiBoot`, and `crLanes.code: [reviewer]`. Q-0144's design phase has traction (3 `.pen` files tracked in charuy) but Q-0145 `ui-reviewer` and Q-0146 `render-compare` have zero installs, so a third sibling lane was carved back to the roadmap rather than built. Worth a habit at triage: for an entry that extends a feature, check whether the feature it extends is switched on in any repo before sizing the work. A `pnpm noldor doctor` row or a triage-time hint that reports "the parent feature's opt-in is unset in every known consumer" would make that check mechanical instead of remembered. (found 2026-08-25 deciding to park Q-0180) [triaged 2026-08-25 → parent-feature-opt-in-check-before-sizing]
- naimplementovat 2. způsob ověřování UI (pen) oproti implementaci -> nějaké efekty pen neumí (svg) takže nemá smysl pixep perfect porovnání (to nechat pro jiné případy), ale udělat ještě 2. možnost porovnání zaměřenou primárně na alignment prvků, font-size, margins/paddings,.. [triaged 2026-08-25 → geometry-compare-lane-the-automated-half]
- (semi)mandatory architecture diagram (based on 4C) when there is full new FD in mermaid [triaged 2026-08-25 → mandatory-c4-diagram-for-new-feature-mds]
- jak funguje práce s .pen fily? máme baseline -> to je super -> proč design features jsou rovnou v /archive -> chceme potvrzení designu frámci implementation flow a až pak implementaci -> otevření .pen filu který se edituje agentem [triaged 2026-08-25 → design-approval-step-before-implementation]
- The pencil MCP ignores `filePath` and edits whatever canvas the app has open, and nothing in the UI-design step says to check. Passing a path that did not exist (a worktree file, while the app held `docs/design/ui/baseline/app.pen`) silently edited the baseline instead — deleting four pages from it — and the app later auto-saved that same session document over two tracked files: the baseline and `docs/design/ui/archive/2026-08-24-liquid-glass-ui.pen`, another feature's archived artifact. Both were byte-identical afterwards, which is how it was spotted; `git status` in the main workspace was the only signal, and the worktree's own status was clean the whole time. Wanted: the step opens with a `get_app_state` assertion that the active canvas IS the file about to be written (the tool already reports it), and says plainly that a wrong path is a silent write to the wrong file rather than an error. A `.pen` write-guard in `checks shared-files` would catch the second half — an edit to `baseline/` or any `archive/` `.pen` from a feature session is never intentional. (found 2026-08-25, gate Step 1.5 on agent-camera-control) [triaged 2026-08-25 → pen-write-target-guard]
- The seeding recipe in the UI-design step is not implementable as written. It says "copy its pages from `docs/design/ui/baseline/<surface>.pen` via pencil MCP, naming them `BASE:<surface>: <name>`", but `execute` has no cross-file copy — `Copy(path, parent)` is within one document — and the baseline is ~1500 nodes, so `Get`-then-`Insert` is not a real option either. What actually works is a filesystem `cp` of the baseline followed by page renames. The naming half is also stale: `capture-ui` emits `FINAL:app: <state>` (`PAGE_PREFIX`, `scripts/design/states.ts`), so a freshly captured baseline carries `FINAL:` pages that must be renamed to `BASE:` before they can serve as the seed — and the committed baseline carried `BASE:` names plus three hand-authored `VAR:` pages and a `FINAL:`, i.e. someone had already renamed and hand-edited a file the memory rule says is generated and must never be hand-edited. Wanted: say `cp` (or ship a `design ui-seed <slug>` that does it), and reconcile who owns page prefixes between the recorder and the design step. (found 2026-08-25) [triaged 2026-08-25 → ui-design-step-pencil-recipe-corrections]
- Newly created `.pen` nodes render blank in the call that creates them, which reads as a broken design rather than a lag. Fresh `text` and `path` nodes came back invisible in `TakeScreenshot` from the same `execute`; frames with a `fill` rendered, and `Copy` of an existing node rendered immediately. Cost roughly six exploratory calls and a wrong conclusion ("the renderer cannot draw text") before the pattern was clear: screenshot in a FOLLOW-UP call, or build from copies. The pen skill says the opposite — "screenshots reflect the changes made earlier in the same execute call" — so the guidance actively misleads here. Wanted: one line in the UI-design step saying to verify in a separate call, since agents will otherwise diagnose their own correct schema as broken. (found 2026-08-25) [triaged 2026-08-25 → ui-design-step-pencil-recipe-corrections]
- On attach paths the UI verdict is computed from the parent FD's entire `links.code`, so an enhancement inherits `required` from files it does not touch. `agent-camera-control` matched six paths — `App.tsx`, `ViewportArea.tsx`, three `packages/viewport/src/*` files and `useMeshData.ts` — none of which the enhancement had to touch to add two agent verbs; the roadmap block carried no `Touches:` clause, so `sessionUiVerdict` had nothing narrower to read. The verdict happened to be right that time (the work grew a toolbar control), but it was right by accident. Wanted: on `*-attach`, take candidate paths from the source block's `Touches:` and fall back to the parent's `links.code` only when the block has none — and say in the step that a `required` verdict with no visual delta is legitimately answered by recording that, rather than by seeding a `.pen` that documents an unchanged surface. (found 2026-08-25) [triaged 2026-08-25 → attach-path-ui-verdict-reads-touches]
- `design:capture-ui` had been broken for days and both the freshness check and CI reported healthy. Two capture states drove through buttons that `ai-first-ui-hide-low-level-tools-by-default` had moved behind a disclosure, so the run died on state 8 of 10 and the temp-then-rename write left the committed baseline in place — describing a toolbar that no longer existed. `checks ui-design-freshness` said `fresh` throughout, because it compares commit ordering, not content, and cannot see that the generator itself fails. The block was only found because a UI-bearing session tried to seed from the baseline. Wanted: the freshness check runs the capture (or a cheap dry-run of its drivers) rather than trusting timestamps, or capture runs in CI on any `uiPaths` diff so the recorder breaks loudly at the commit that breaks it. Related: `scripts/design/__tests__/validate.test.ts` opens a hardcoded `/Applications/Pencil.app/...` path unguarded and fails wherever Pencil is installed elsewhere, while the capture script guards the same path — so the one test that would have covered the schema was already red and ignored. (found 2026-08-25) [triaged 2026-08-25 → ui-baseline-freshness-must-run-the-capture]
- The gate prose does not pre-empt the `Noldor-Sibling-Scope` trailer. A commit that touches `src/**` *and* `docs/noldor/**` needs a `Noldor-Sibling-Scope: noldor:<page>` trailer, and the `noldor-scope` hook only says so after the commit has already been rejected — the mechanism is documented in `docs/noldor/git-and-commits.md`, but nothing puts it in front of the operator at the moment it applies. Worth pre-empting in the gate prose for any change whose fix spans code plus its runner-neutral doc twin, or auto-suggesting the trailer at stage time rather than at reject time. (found 2026-08-24 shipping Q-0158, absorbed 2026-08-25) [triaged 2026-08-25 → gate-prose-should-pre-empt-the-sibling-scope-trailer]
- The clones ratchet and the clones *group* check disagree on attribution: `clones check` printed `no clone group touches this change - green` while the same run reddened on `duplicated tokens rose 27735 -> 27845 (+110)`. The rise came from a new test reusing its file's established 15-instance scaffold (`const commits: Commit[] = [...]` + `checkCrGate({...runGit: makeGitFake(commits)})`), i.e. exactly the idiom consistency the test rules ask for. So any PR that adds a test to a table-driven test file owes a baseline re-record commit, and the check itself declines to name the offending lines — the operator has to diff group lists between branches to find them, and a green run prints no group list to diff against. Either the ratchet should exempt `**/__tests__/**` (or weight it separately), or `clones check` should name which files moved the total the way `microChoreOffenders` names its offenders. Related to Q-0165 (preflight/hook disagreement) but distinct: this is the standalone CLI disagreeing with itself in one run. (found 2026-08-25 shipping Q-0164, absorbed 2026-08-25) [triaged 2026-08-25 → clones-ratchet-and-clone-group-check-disagree-on-attribution]
- The plan-split rules can manufacture public API. `noldor-plan` step 6 caps a part at 1000 rows and requires each `-part<N>` to be independently shippable software, and the only way to satisfy both on a feature whose payload is one internal lane is to invent CLI surfaces: the geometry-compare plan grew `design geometry-validate`, `design geometry-diff`, `design geometry-export` and `design geometry-review` — four commands, four manifest rows, four twinned catalog entries — purely so each part could register a runnable surface. Two of them turned out genuinely useful (they are what let the feature ship at all when the lane was parked), which is why this is a trade-off to state rather than a bug to fix: the vertical rule buys shippability at the price of API surface, and nothing in the guidance says to weigh that. Wanted: a note in the P1 remedy prose that a part may also be shippable as an internal capability behind an existing entry point, plus an explicit "is this command worth owning forever?" check before a split mints one. (found 2026-08-25 planning Q-0180) [triaged 2026-08-25 → plan-split-guidance-permits-a-part-that-ships-nothing]
