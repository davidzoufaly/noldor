// Library re-exports populated as modules are migrated (Task 3 onward adds
// `export * from './<group>/...'` lines here). Until then this module is empty.
export type NoldorLibrary = Record<string, never>;
