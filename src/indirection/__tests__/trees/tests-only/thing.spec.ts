// @tests: abstraction-cost-ratchet
// Fixture, not a real test. Its `.spec.ts` name is the point: this tree exists
// to prove a corpus of nothing but test files reads as `empty` rather than
// `unmeasurable`. The tag is required because the test-tag gate matches on the
// filename pattern, which this file must keep.
export const thing = 1;
