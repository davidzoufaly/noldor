---
description: Noldor triage — bulk-triage ideas.md into roadmap/backlog
---

Run the Noldor triage flow. Read `docs/vision.md` + `docs/noldor/triage.md`, then:

1. Read untriaged bullets in `ideas.md`.
2. Propose target (roadmap/backlog) + area + size + impact per bullet; batch-confirm.
3. Write schema-C blocks; use `pnpm noldor triage merge-candidates` to fold duplicates.
4. Append `[triaged YYYY-MM-DD → slug]` markers back into `ideas.md`.
