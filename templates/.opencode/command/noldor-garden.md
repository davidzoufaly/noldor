---
description: Noldor garden — doc gardening pass (stale plans, rule contradictions, SDD gaps)
---

Run the Noldor garden flow. Read `docs/noldor/garden-and-drift.md`, then
`pnpm noldor garden-detect` to surface stale plans, unused backlog entries, rule-pair
contradictions, and SDD gaps. Present an operator-confirmed checklist; on confirm run the
safe auto-actions + the regen chain. Operator commits.
